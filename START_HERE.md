# 🚀 시작하기

## 🕷️ 웹 크롤링이란?

**웹 크롤링(Web Crawling)**은 웹 페이지를 자동으로 탐색하고 데이터를 수집하는 기술입니다.
- 웹 페이지 요청 → HTML 파싱 → 데이터 추출 → 저장
- 뉴스, 상품 정보, 가격 비교 등 다양한 데이터 수집에 활용
- **중요**: robots.txt 확인, 요청 간격 조절, 저작권 준수 필수!

자세한 내용은 README.md의 "웹 크롤링이란?" 섹션을 참고하세요.

---

## BeautifulSoup4란?

**BeautifulSoup4**는 HTML/XML 문서를 파싱하고 탐색하는 Python 라이브러리입니다.
- 복잡한 HTML에서 원하는 데이터를 쉽게 추출
- CSS 선택자, 태그명 등으로 직관적으로 요소 탐색
- 깨진 HTML도 자동 수정하여 파싱 가능

**왜 사용하나요?** 정규표현식보다 훨씬 간단하고, 웹 개발에 익숙한 CSS 선택자를 그대로 사용할 수 있어서 편리합니다!

자세한 내용은 README.md의 "BeautifulSoup4란?" 섹션을 참고하세요.

---

## 첫 번째 단계

1. **패키지 설치**
   ```bash
   pip install -r requirements.txt
   ```

   ### 📦 pip 라이브러리 확인 명령어
   
   패키지 설치 후 확인하거나 관리할 때 유용한 명령어들입니다:
   
   | 명령어 | 설명 | 예시 |
   |--------|------|------|
   | `pip list` | 설치된 모든 패키지 목록 보기 | `pip list` |
   | `pip show 패키지명` | 특정 패키지 상세 정보 확인 | `pip show beautifulsoup4` |
   | `pip freeze` | requirements.txt 형식으로 출력 | `pip freeze` |
   | `pip freeze > requirements.txt` | 현재 패키지 목록을 파일로 저장 | `pip freeze > requirements.txt` |
   | `pip list \| findstr 패키지명` | 특정 패키지 검색 (Windows) | `pip list \| findstr beautifulsoup4` |
   | `pip list \| grep 패키지명` | 특정 패키지 검색 (Linux/Mac) | `pip list \| grep beautifulsoup4` |
   | `pip install -r requirements.txt --dry-run` | 설치 전 확인 (실제 설치 안 함) | `pip install -r requirements.txt --dry-run` |
   
   **자주 사용하는 명령어:**
   - `pip list` - 전체 패키지 목록 확인
   - `pip show 패키지명` - 특정 패키지 정보 확인
   - `pip list \| findstr 패키지명` - 패키지 설치 여부 빠르게 확인

2. **프로젝트 구조 이해**
   - `config.py` - 설정 파일
   - `utils.py` - 유틸리티 함수
   - `scraper.py` - 크롤링 로직
   - `pipeline.py` - 데이터 처리 및 저장
   - `main.py` - 실행 파일

3. **학습 순서**
   - README.md의 Q1부터 Q7까지 순서대로 진행
   - 각 질문에 답하면서 코드 작성
   - 막히면 힌트를 참고하세요

4. **안전한 크롤링 사이트 선택**
   - **추천**: `https://quotes.toscrape.com` (크롤링 연습용 사이트)
   - 다른 추천 사이트는 README.md의 Q3 섹션 참고

## 빠른 시작 체크리스트

### Step 1: config.py 완성
- [ ] NEWS_URL 설정 (추천: "https://quotes.toscrape.com")
- [ ] USER_AGENT 설정
- [ ] SAVE_PATH 설정
- [ ] REQUEST_DELAY 설정
- [ ] SAVE_FORMAT 설정

### Step 2: utils.py 완성
- [ ] setup_logging() 함수
- [ ] ensure_directory() 함수
- [ ] delay() 함수

### Step 3: scraper.py 완성
- [ ] __init__() 메서드
- [ ] fetch_page() 메서드
- [ ] parse_html() 메서드
- [ ] extract_headlines() 메서드
- [ ] scrape() 메서드

### Step 4: pipeline.py 완성
- [ ] __init__() 메서드
- [ ] clean_data() 메서드
- [ ] save_to_json() 메서드
- [ ] save_data() 메서드
- [ ] run() 메서드

### Step 5: main.py 완성
- [ ] import 문
- [ ] 로깅 설정
- [ ] main() 함수

## 디버깅 팁

1. **로깅 활용**: 각 단계에서 로그를 출력하여 진행 상황 확인
2. **작은 단위 테스트**: 각 함수를 개별적으로 테스트
3. **에러 메시지 확인**: Python의 에러 메시지를 자세히 읽어보세요
4. **웹사이트 구조 확인**: 브라우저 개발자 도구(F12)로 HTML 구조 파악

## 도움이 필요할 때

1. BeautifulSoup4 문서: https://www.crummy.com/software/BeautifulSoup/bs4/doc/
2. Requests 문서: https://requests.readthedocs.io/
3. Python 공식 문서: https://docs.python.org/3/

**화이팅! 💪**
